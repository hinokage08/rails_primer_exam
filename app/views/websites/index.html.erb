<h2>自分のツイート</h2>

<table>
<% @websites.each do |website| %>
  <tr>
    <td><%= website.content %></td>
    <td><%= link_to '詳細を確認する', website_path(website.id) %></td>
  </tr>
<% end %>
</table>
<%= link_to '新しいツイートをする', new_website_path %>
