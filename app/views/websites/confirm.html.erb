<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @website, url: websites_path, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <p>本文:<%= @website.content %></p>
  <%= form.submit  "ツイートする" %>
<% end %>
